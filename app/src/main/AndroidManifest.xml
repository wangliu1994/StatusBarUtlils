<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.winnie.utils.statusbarutlils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StatusBarActivity1"
            android:theme="@style/AppTheme.TranslucentStatus1" />

        <activity
            android:name=".StatusBarActivity2"
            android:theme="@style/AppTheme.TranslucentStatus2" />

        <activity
            android:name=".StatusBarActivity4"
            android:theme="@style/AppTheme.TranslucentStatus2" />

        <activity
            android:name=".StatusBarActivity3"
            android:theme="@style/AppTheme.TranslucentStatus3" />

        <activity
            android:name=".StatusBarActivity5"
            android:theme="@style/AppTheme.TranslucentStatus5" />
    </application>

</manifest>